# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Connection (use MongoDB Atlas free tier)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vehicle-tracking

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# File Upload Directory
UPLOAD_DIR=uploads

# Uptime Robot will ping this endpoint to keep server awake on Render
# Configure Uptime Robot to ping: https://your-app.onrender.com/api/health

# Vehicle offline threshold (minutes)
OFFLINE_THRESHOLD_MINUTES=5

# GPS update interval expected from drivers (seconds)
GPS_UPDATE_INTERVAL=5
